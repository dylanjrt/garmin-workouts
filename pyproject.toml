[project]
name = "garmin-workout-creator"
version = "0.1.0"
description = "Create and upload swimming workouts to Garmin Connect"
requires-python = ">=3.11"
dependencies = [
    "garminconnect>=0.2.38",
    "python-dotenv>=1.2.1",
]

[project.scripts]
swim-upload = "upload:main"
